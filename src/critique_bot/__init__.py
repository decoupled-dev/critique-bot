"""Headless Edge client that drives a web LLM chat UI.

Default mode is a specialized code reviewer; ``--mode general`` sends an
arbitrary prompt and optional files; ``--mode chat`` is an interactive
terminal session.
"""

__version__ = "0.1.0"
